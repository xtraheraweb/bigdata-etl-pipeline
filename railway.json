{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "dockerfile"
  },
  "deploy": {
    "startCommand": "bash -c 'pip install apache-airflow-providers-docker && airflow webserver --port 8080'",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300
  }
}
